#include "testpiemenu.h"

void TestPieMenu::openAndCloseMenu()
{
    /*
    // Arrange
    PieMenu *menu = new PieMenu();
    bool before, middle,  after;

    // Act
    before = menu->isOpen();
    menu->open();
    middle = menu->isOpen();
    menu->close();
    after = menu->isOpen();

    // Assert
    Q_ASSERT(before == false);
    Q_ASSERT(middle == true);
    Q_ASSERT(after == false);
    */
}
